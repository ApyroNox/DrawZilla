var config = {
	MediaObserver: {
		'prefers-color-scheme': false,
		orientation: true,
		device: true,
	},
	ScalingDetails: {
		reservedHeigth: 56,
		reservedWidth: 0,
		padding: 16,
		aspectRatio: {
			width: 4,
			height: 3,
		},
	},
};

window.addEventListener('load', () => {});
window.addEventListener('beforeunload', () => {}, false);
